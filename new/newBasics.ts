type Pair<T> = [first: T, T];
let array: string[] | number[] = [];

array.filter((X) => !!X);

type Person = {
  name: string;
  email: string;
};

const person: Person = {
  name: "Usman",
  email: "usman@yahoo.com",
};

window.open("www.google.com");
